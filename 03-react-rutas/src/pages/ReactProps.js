import React from "react";

const ReactProps = () => {
  return (
    <div>
      <h4>Props</h4>
      <p>
        Son valores que recibe un componente hijo de uno padre. Se agrupan en un
        objeto llamado <b>PROPS</b>, el cual puede recibir diferentes tipos de
        datos, como:
      </p>
    </div>
  );
};

export default ReactProps;
